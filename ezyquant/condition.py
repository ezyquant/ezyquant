import pandas as pd


def rank_factor(
    factor_df: pd.DataFrame, quantity: int, ascending: bool = True
) -> pd.DataFrame:
    return pd.DataFrame()


def filter_factor(
    factor_df1: pd.DataFrame, operator: str, factor_df2: pd.DataFrame
) -> pd.DataFrame:
    return pd.DataFrame()
